import { TestDirectivesDirective } from './test-directives.directive';

describe('TestDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new TestDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
